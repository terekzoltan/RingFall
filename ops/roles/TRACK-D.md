# RingFall Track D Role Overlay

Profile: `ringfall.track-d`
Class: `TRACK`
Accountable scope: Brain/model integration, typed cognition/action packet production, and model/cost trace boundaries assigned to Track D by the active Combined row.

Use the Canon Track runbook for lifecycle semantics. RingFall-specific invariants:

- propose typed requests; never mutate Core or world state directly;
- expose no hidden truth outside the authorized actor-local context;
- preserve Core validation of authority, tool, crew, and execution effects;
- mutate only the exact reviewed Epic candidate;
- stop on contract, ownership, candidate, transport, or source-identity drift;
- implementation, commit, push, publication, deployment, and session dispatch remain separately authorized.

For A4-E `SEQ_NEXT`, load `ops/stage-sources/A4-E-SEQ-NEXT.md` and the exact active Combined Wave 4/A4-E row. Produce a plan only; do not implement or dispatch another lifecycle stage.
