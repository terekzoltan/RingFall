# Example runtime config. Copy to configs/local.runtime.yaml for local overrides.
run_mode: dev_cheap
artifact_root: data/runs
scenario_root: scenarios
model_policy_path: configs/model-policy.example.yaml
provider_policy_path: configs/provider.example.yaml
default_provider: mock
allow_real_provider_calls: false
