# Ringfall Brain Step 1

This package is the Wave 3 Step 1 brain skeleton for local CLI and model-policy fixture checks.

Supported invocation contracts for this step:

- From the package root: `cd src/ringfall-brain`; `python -m ringfall_brain.cli --help`
- From the repository root with explicit import context: `$env:PYTHONPATH="src/ringfall-brain"; python -m ringfall_brain.cli --help`

Step 1 does not provide an installed console script, package install contract, CI lane, provider/API call path, credential handling, prompt runtime, trace writer, or cost writer.
